// Nutree Pro is currently the only plan; kept separate from the localized
// display strings in dictionaries.ts / subscribeContent.ts since GA4 params
// need a plain number and an ISO currency code, not a localized label.
export const PLAN_PRICE = 129;
export const PLAN_CURRENCY = "SAR";
